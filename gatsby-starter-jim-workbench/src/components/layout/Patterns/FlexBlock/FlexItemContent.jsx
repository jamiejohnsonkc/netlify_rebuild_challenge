/** @jsx jsx */
import { jsx, Flex, Box } from "theme-ui"
import React from "react"
import PropTypes from "prop-types"

const FlexItemContent = (props) => <div>{props.children}</div>

export default FlexItemContent
